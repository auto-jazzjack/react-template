import React from "react";
import MainLayout from '@mui/material'

type Props = {
    title: string;
    content: string;
}

export const BoardList = ({}:Props) =>{
    return (
        <MainLayout>

        </MainLayout>
        
    )
}